const worker = require('worker_threads');

const runJob = (ms, fileName, ) => {
    let workerData = {
        file:
    };
    const worker = new Worker('./src/utils/generalWorker.js', { func })
};

module.exports = {
    runJob,
}